import 'package:flutter/material.dart';
class FastFoodPage extends StatelessWidget {
  const FastFoodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Fast Food Page"),
      ),
    );
  }
}
