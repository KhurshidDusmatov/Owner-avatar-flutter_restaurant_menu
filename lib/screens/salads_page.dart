import 'package:flutter/material.dart';
class SaladsPage extends StatelessWidget {
  const SaladsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Salads Page"),
      ),
    );
  }
}
